# PSL directives
# Currently, PSL is not supported, but will produce some tokens.

PSLDirective: !Builtin

PSLPropertyDeclaration: !Builtin

PSLSequenceDeclaration: !Builtin

PSLClockDeclaration: !Builtin

PSLVerificationUnit: !Builtin
