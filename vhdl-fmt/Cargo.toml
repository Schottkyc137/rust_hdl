[package]
name = "vhdl-fmt"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.54", features = ["derive"] }
vhdl_syntax = { version = "0.1.0", path = "../vhdl_syntax", default-features = false }

[dev-dependencies]
insta = "1.46.0"
