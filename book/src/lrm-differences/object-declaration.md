# Object Declaration

The LRM's `ObjectDeclaration` only groups the four object declaration forms constant, signal, variable, and file.
It is not referenced anywhere and was therefore dropped in `vhdl_syntax`:

```diff
- ObjectDeclaration
```
